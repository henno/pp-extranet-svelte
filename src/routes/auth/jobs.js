import * as db from "../../lib/models/userModel.ts"

export const post = async ({request}) => {

return {
        body: {
            "response": 'test response'
        },
        status: 200
    }

}
